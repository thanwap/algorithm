"use strict";

var _countChar = require("./count-char");

test('should count char in normal string', function () {
  //   let actual = countChar('hello');
  expect(1).toBe(1); //   expect(Object.keys(actual), 4);
});